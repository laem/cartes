'use client'
import styled from 'styled-components'

export const IconStyle = styled.ul`
	margin: 1.3rem auto;
	width: 3.5rem;
	list-style-type: none;
	li {
		height: 0.4rem;
		margin: 0.2rem 0;
		background: var(--lighterColor);
	}
`
